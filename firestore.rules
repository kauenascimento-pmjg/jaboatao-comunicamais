rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Permitir qualquer leitura/gravação se o usuário estiver LOGADO (para teste de integração)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
